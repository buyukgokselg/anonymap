import 'package:flutter/material.dart';

class AppColors {
  // ── PulseCity Ana Renkler ──
  static const primary = Color(0xFFE94560);
  static const primaryDark = Color(0xFF1A1A2E);
  static const primaryGlow = Color(0xFFFF6B81);

  // ── Arka Planlar ──
  static const bgMain = Color(0xFF0A0A1A);
  static const bgCard = Color(0xFF12122A);
  static const bgChip = Color(0xFF0F0F24);
  static const bgMap = Color(0xFF0A0A1A);
  static const bgGlass = Color(0x0AFFFFFF);
  static const bgSurface = Color(0xFF16162E);

  // ── Accent Renkler ──
  static const accent = Color(0xFF0F3460);
  static const accentLight = Color(0xFF533483);
  static const neonCyan = Color(0xFF00D9FF);

  // ── Intent Mod Renkleri ──
  static const modeKesif = Color(0xFFE94560);
  static const modeSakinlik = Color(0xFF2ECC71);
  static const modeSosyal = Color(0xFF3498DB);
  static const modeUretkenlik = Color(0xFFF39C12);
  static const modeEglence = Color(0xFF9B59B6);
  static const modeAcikAlan = Color(0xFF1ABC9C);
  static const modeTopluluk = Color(0xFFE67E22);
  static const modeOzelCevre = Color(0xFF7F8C8D);

  // ── Pulse Score Renkleri ──
  static const pulseVeryLow = Color(0xFF2C3E50);
  static const pulseLow = Color(0xFF3498DB);
  static const pulseMedium = Color(0xFFF39C12);
  static const pulseHigh = Color(0xFFE67E22);
  static const pulseVeryHigh = Color(0xFFE94560);

  // ── Density Seviyeleri ──
  static const densityEmpty = Color(0xFF1A1A2E);
  static const densityLow = Color(0xFF2980B9);
  static const densityMedium = Color(0xFFF39C12);
  static const densityHigh = Color(0xFFE67E22);
  static const densityCrowded = Color(0xFFE94560);

  // ── Metin Renkleri ──
  static const textPrimary = Colors.white;
  static const textSecondary = Color(0xFF8A9BB5);
  static const textHint = Color(0xFF3D4F6B);
  static const textMuted = Color(0xFF555E70);

  // ── Diğer ──
  static const divider = Color(0xFF1C1C3A);
  static const error = Color(0xFFFF4D6D);
  static const success = Color(0xFF2ECC71);
  static const warning = Color(0xFFF39C12);

  // ── Eski isimler (geçiş dönemi — ekranlar yenilenince kaldırılacak) ──
  static const modeTakiliyorum = Color(0xFFFF9100);
  static const modeArkadas = Color(0xFF7C4DFF);
  static const modeGizli = Color(0xFF455A64);
  static const user1 = Color(0xFFFF4D6D);
  static const user2 = Color(0xFF7C4DFF);
  static const user3 = Color(0xFFFF9100);
  static const user4 = Color(0xFF00BCD4);
  static const neonGreen = Color(0xFF00E678);
}